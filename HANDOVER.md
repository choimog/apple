# 인수인계 문서 (HANDOVER)

**서점 베스트셀러 통합 트래킹 시스템**
최종 갱신: 2026-08-07

이 문서는 **개발 지식이 없는 분**이 읽고 운영할 수 있게 썼습니다.
새 PC에서 이어서 작업하실 때, 또는 다른 사람에게 넘기실 때 이 문서 하나만 보시면 됩니다.

---

## 1. 이게 뭔가요? (한 줄 요약)

교보문고·예스24·알라딘의 **베스트셀러를 매일 아침 자동으로 모아서**,
같은 책끼리 묶어 3사 순위를 한 화면에서 비교하는 사이트입니다.

**대표님 PC가 꺼져 있어도 100% 클라우드에서 알아서 돌아갑니다.**

---

## 2. 지금 어디까지 됐나요?

| 단계 | 내용 | 상태 |
|---|---|---|
| Phase 0 | 요구사항 확정 | ✅ 완료 |
| Phase 1 | 데이터베이스 만들기 | ✅ 완료 |
| Phase 2 | 매일 자동 수집 (알라딘·예스24) | ✅ 완료 |
| Phase 3 | 교보문고 수집 + 같은 책 묶기 | ✅ 완료 |
| Phase 4 | 사이트 (순위표·검색·상세·상태) | ✅ 배포·보안설정 완료 |
| Phase 5 | 로그인·계정·공유 링크 | ⬜ 아직 |
| Phase 6 | AI 일일 인사이트 리포트 | ⬜ 아직 |

### 사이트 주소

Vercel 에 배포돼 있습니다. **고정 주소**(배포해도 안 바뀌는 주소)를 쓰세요.

- Vercel → 프로젝트 → **Deployments** → 위쪽에 `Production` 이라고 표시된
  배포의 주소가 고정 주소입니다.
- 시크릿 창(로그아웃 상태)에서 열어봤을 때 보이면 팀에 공유해도 됩니다.

> Production Branch 를 `claude/bookstore-bestseller-tracker-projtf` 로
> 설정해 두었으므로, 앞으로 코드를 고치면 자동으로 정식 배포됩니다.

### 다음에 정할 것

| 항목 | 내용 |
|---|---|
| **저장공간 이전** | 🚨 주간이 더해져 **약 2주**면 무료 용량이 찹니다. [docs/archive-setup.md](docs/archive-setup.md) 를 이번 주 안에 한 번 해주세요. (10분, 0원) |
| Phase 5 | 로그인·계정·공유 링크 |
| Phase 6 | AI 일일 인사이트 리포트 |

---

## 3. 지금 모이고 있는 데이터

2026-08-07 기준 실측입니다.

| 서점 | 분야 | 수집량 | 판매지수 | ISBN |
|---|---|---|---|---|
| 알라딘 | 온라인 4개 | 4,000권 | ✅ 있음 | ❌ 없음 |
| 예스24 | 온라인 | 3,992권 | ✅ 있음 | ❌ 없음 |
| 교보문고 | 온라인 3개 + 매장 4곳 | 800권 | ❌ 미제공 | ✅ **있음** |

- **총 8,244권** (중복 제거 후)
- **표지 주소 보유율 100%** (8,208/8,244)
- **분야 15개**

### 같은 책 묶기 결과 (2026-08-07 실측)

| | |
|---|---|
| 서점별 도서 | 8,244권 |
| **묶은 뒤 도서 종수** | **6,028종** |
| 2개 이상 서점에서 발견된 책 | 1,442종 |
| 저장된 매칭 근거 | 3,033건 |
| 걸린 시간 | 24.9초 |

같은 날 다시 돌려도 **아무것도 바뀌지 않습니다** (새로 0종 · 연결 0건).
= 몇 번을 실행해도 데이터가 망가지지 않습니다.

> 📌 ISBN 은 교보만 얻을 수 있습니다. 교보 표지 이미지 주소 안에 13자리가
> 들어 있어서, 상세 페이지에 안 들어가고도 읽을 수 있습니다.
> 나머지 두 서점은 목록에 ISBN 을 노출하지 않습니다.

---

## 4. 매일 뭐가 자동으로 돌아가나요?

| 시각(한국) | 작업 | 어디서 보나요 |
|---|---|---|
| **오전 6시** | 3사 베스트셀러 수집 (일간 + 주간) | GitHub → Actions → `매일 수집 (daily crawl)` |
| **오전 9시** | 같은 책끼리 묶기 | GitHub → Actions → `도서 매칭` |
| 상시 | 사이트가 10분마다 새 데이터 반영 | 사이트 |

> 📌 묶기가 7시 → **9시**로 늦춰졌습니다 (2026-08-07).
> 주간 베스트셀러가 더해져 수집이 1시간 안에 안 끝날 수 있는데,
> 수집 도중에 묶기가 시작되면 그날 자료의 절반만 보고 묶게 됩니다.

### 일간과 주간, 둘 다 매일 모읍니다

| | 일간 | 주간 |
|---|---|---|
| 무엇 | 어제 **하루** 판매 순위 | 최근 **7일 누적** 판매 순위 |
| 쓸모 | 신간 반응·이벤트 효과가 바로 보임 | 하루 출렁임이 걸러진 실력이 보임 |
| 사이트에서 | 분야 목록 위 **[일간]** 선택 | 분야 목록 위 **[주간]** 선택 |

주간도 **매일** 찍습니다. 서점의 주간 집계는 날마다 갱신되므로,
매일 모아 두면 "7일 누적 순위가 어떻게 움직였는지"를 볼 수 있습니다.

> ⚠️ 일간과 주간은 분야 이름이 똑같습니다(둘 다 '전체').
> 사이트 순위표 제목과 내려받는 파일 이름에 **일간/주간이 항상 표시**되니
> 그걸 보고 구분하세요.

**실패하면 GitHub 이 대표님 이메일(hssh8159@gmail.com)로 알려줍니다.**

> ⚠️ GitHub 무료 스케줄러는 사용량이 몰리면 5~30분 늦게 시작될 수 있습니다.
> "6시 정각"이 아니라 "6시~6시 30분 사이"로 이해해 주세요.

---

## 5. 자주 하실 일 — 개발 지식 없이 하는 방법

### (1) 수집할 분야·매장을 추가하고 싶을 때

`config/sources.yaml` 파일만 고치면 됩니다.

1. https://github.com/choimog/apple 접속
2. `config` 폴더 → `sources.yaml` 클릭
3. 오른쪽 위 연필(✏️) 클릭
4. 기존 줄을 **통째로 복사해서 붙여넣고** 값만 바꾸기
5. 맨 아래 초록색 **Commit changes** 클릭

> ⚠️ 들여쓰기(왼쪽 여백)는 **스페이스만** 쓰세요. Tab 키는 오류가 납니다.
> 기존 줄을 복사해서 쓰는 게 가장 안전합니다.

### (2) 서점 화면이 바뀌어서 수집이 실패할 때

이메일로 실패 알림이 오고, 사이트의 **수집 상태** 화면에도 빨간색으로 나옵니다.

고칠 파일: `config/selectors.yaml`
이 파일에는 **실제 서점 HTML 이 한글 주석으로 그대로 적혀 있습니다.**
개발자에게 이 파일과 실패 로그를 보여주면 바로 고칠 수 있습니다.

### (3) 같은 책인데 따로 나올 때 / 다른 책인데 묶였을 때

기준을 조정하는 파일: `config/matching.yaml`
설명 문서: [docs/matching-rules.md](docs/matching-rules.md)

고친 뒤에는 **GitHub → Actions → `도서 매칭` → Run workflow** 를 한 번 눌러야 반영됩니다.

### (4) 지금 당장 수집을 돌리고 싶을 때

GitHub → Actions → **매일 수집 (daily crawl)** → 오른쪽 **Run workflow**

- `store` : 특정 서점만 (`kyobo` / `yes24` / `aladin`). 비우면 전체
- `dry_run` : `true` 로 하면 저장 안 하고 확인만

---

## 6. 절대 지키고 있는 원칙

대표님이 처음에 못 박으신 것들입니다. 코드에 그대로 반영돼 있습니다.

| 원칙 | 어떻게 지키고 있나 |
|---|---|
| **서점 공식 API 사용 금지** | 알라딘 TTB 포함, 어떤 공식 API 도 안 씁니다 |
| **도서 상세 페이지 진입 금지** | 목록 페이지만 읽습니다. 예외 없음 |
| **표지 이미지 저장 금지** | 주소만 문자열로 보관. 브라우저가 서점에서 직접 불러옵니다 |
| **robots.txt 준수** | 매 실행마다 다시 확인. 금지면 그 서점 수집을 중단합니다 |
| **요청 간격 1~2초** | 동시 접속 없이 한 번에 하나씩 |
| **가짜 데이터 금지** | 없는 값은 비워 둡니다. 실패는 실패로 기록합니다 |
| **비밀키 코드에 금지** | 환경변수·Secrets 에만. 커밋 전 매번 검사했습니다 |
| **조용한 실패 금지** | 수집량이 평소 절반 미만이면 성공이 아니라 **실패**로 기록 |

### ⚠️ 교보문고에 대한 예외 — 대표님 승인 사항 (2026-08-07)

교보 베스트셀러 데이터는 **robots.txt 가 금지한 `/api/gw` 경로에서만** 나옵니다.
허용된 다른 입구가 있는지 확인했지만 **없었습니다** (www / mobile / product 모두 404).

이 사실을 보고드렸고, 대표님이 **"헤드리스 브라우저로 진행"** 을 선택하셨습니다.
브라우저를 띄우면 페이지의 자바스크립트가 그 경로를 자동으로 호출합니다.

대신 예의는 더 엄격하게 지킵니다:
- 신원을 밝히는 User-Agent
- 페이지 사이 1.5~2.5초, 동시 접속 없음
- 광고·추적 요청은 아예 차단, 이미지는 받지 않음(주소만 읽음)

> 이 결정을 되돌리시려면 `crawler/run_daily.py` 의 `BROWSER_STORES` 에서
> `"kyobo"` 를 빼면 교보만 수집이 멈추고 나머지는 그대로 돕니다.

---

## 7. 지금 드는 돈

| 항목 | 요금제 | 월 비용 |
|---|---|---|
| GitHub Actions | 무료 (공개 저장소는 무제한) | **0원** |
| Supabase | 무료 (500MB) | **0원** |
| Vercel | 무료 (Hobby) | **0원** |
| **합계** | | **0원** |

### ⚠️ 앞으로 돈이 들 수 있는 지점 두 가지

**(1) Supabase 저장공간 — 약 2주 뒤** 🚨 대비책 준비 완료, 설정만 하시면 됩니다

2026-08-07 에 주간 베스트셀러를 더하면서 수집 대상이
**122개 → 208개 분야·매장**이 됐습니다. 하루에 쌓이는 순위 기록도
그만큼 늘어납니다.

| | 분야·매장 | 하루 순위 기록(최대) | 무료 500MB 가 차는 시점 |
|---|---|---|---|
| 8월 7일 이전 | 122개 | 약 9만 건 | 약 1개월 |
| **지금 (일간+주간)** | **208개** | **약 16만 건** | **약 2주** |

(순위 기록 1건이 색인까지 포함해 약 176바이트라는 실측 추정 기준입니다)

대비책을 만들어 두었습니다: **오래된 기록을 Cloudflare R2(무료 10GB)로 이동**
- 데이터를 버리는 게 아니라 옮깁니다. R2 무료 한도로 20년 넘게 씁니다.
- 최근 30일은 Supabase 에 남아 사이트에서 그대로 보입니다.
- 매주 월요일 오전 8시 자동 실행.
- **설정을 한 번 해주셔야 켜집니다** → [docs/archive-setup.md](docs/archive-setup.md)
  (약 10분, 0원. 설정 전에는 아무 일도 안 일어납니다)

**(2) AI 일일 리포트 (Phase 6)**

Claude Sonnet 5 기준 **1회 약 $0.075, 월 약 $2.25 (약 3,300원)**.
대표님이 정하신 월 5,000원 예산 안입니다. 시작 전에 다시 확인받겠습니다.

---

## 8. 파일이 어디에 뭐가 있나요?

```
apple/
├─ HANDOVER.md              ← 지금 이 문서
├─ README.md                ← 프로젝트 소개
│
├─ config/                  ★ 개발 지식 없이 고치는 곳
│   ├─ sources.yaml         수집할 주소 목록 (분야·매장 추가는 여기)
│   ├─ selectors.yaml       서점 화면에서 뭘 읽을지 (화면 개편 시 여기)
│   └─ matching.yaml        같은 책 판단 기준 (배점·단어 목록)
│
├─ db/
│   ├─ schema.sql           데이터베이스 설계도 (Supabase 에 붙여넣어 실행)
│   ├─ rls.sql              ★ 보안 설정 — 읽기만 열고 쓰기·삭제 차단
│   └─ archive_schema.sql   보관소 기록표 (보관소 설정할 때 실행)
│
├─ crawler/                 수집 프로그램 (파이썬)
│   ├─ run_daily.py         ★ 매일 수집 메인
│   ├─ run_match.py         ★ 같은 책 묶기 메인
│   ├─ common/
│   │   ├─ http.py          예의 있는 요청 (간격·재시도·차단 감지)
│   │   ├─ browser.py       화면 없는 브라우저 (교보 전용)
│   │   ├─ robots.py        robots.txt 해석
│   │   ├─ normalize.py     제목·저자·출판사 표기 통일
│   │   ├─ match.py         같은 책 판정 (100점 배점)
│   │   ├─ config.py        설정 파일 읽기
│   │   └─ db.py            데이터베이스 저장
│   ├─ stores/
│   │   ├─ aladin.py        알라딘 읽기
│   │   ├─ yes24.py         예스24 읽기
│   │   └─ kyobo.py         교보문고 읽기
│   └─ probe_*.py           서점 구조 조사용 (평소엔 안 씀)
│
├─ web/                     사이트 (Next.js)
│   ├─ app/                 화면들
│   ├─ components/          화면 조각들
│   └─ lib/queries.ts       데이터 조회
│
├─ docs/
│   ├─ deploy.md            ★ 사이트 올리는 법 + 보안 설정(3.5단계)
│   ├─ archive-setup.md     ★ 보관소(R2) 만드는 법 — 용량 대비
│   └─ matching-rules.md    같은 책 판단 규칙 설명
│
├─ tests/                   자동 검사
└─ .github/workflows/       자동 실행 설정
```

---

## 9. 비밀키는 어디에 있나요?

**코드에는 한 글자도 없습니다.** 아래 두 곳에만 있습니다.

| 열쇠 | 어디에 | 용도 |
|---|---|---|
| `SUPABASE_SERVICE_ROLE_KEY` (관리자) | GitHub → Settings → Secrets → Actions | 수집 작업이 데이터를 **쓸 때** |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` (공개용) | Vercel → Settings → Environment Variables | 사이트가 데이터를 **읽을 때** |

> 🚨 **관리자 열쇠(service_role)를 사이트나 채팅에 절대 넣지 마세요.**
> 그 열쇠가 있으면 누구나 데이터를 지울 수 있습니다.
> 실수로 노출했다면 Supabase → Settings → API → **Reset** 으로 즉시 재발급하세요.

> ℹ️ 현재 저장소는 **공개(public)** 상태입니다. 대표님이 괜찮다고 하셨습니다.
> 코드에 비밀키가 없어서 문제는 없지만, 비공개로 바꾸고 싶으시면
> GitHub → Settings → 맨 아래 **Change repository visibility** 에서 바꿀 수 있습니다.
> 🚨 **비공개로 바꾸면 돈이 듭니다.** 공개 저장소는 Actions 가 무제한 무료지만,
> 비공개는 월 2,000분 제한이 생깁니다. 수집 하나가 하루 1~2시간이라
> 한 달이면 **2,700~3,600분** → 한도를 넘어 초과 요금이 붙습니다.
> 비공개로 바꾸고 싶으시면 먼저 말씀해 주세요. 수집 범위를 줄이거나
> 다른 방법을 같이 찾아야 합니다.

---

## 10. 새 PC 에서 이어서 작업하려면

개발 작업이 필요할 때만 해당됩니다. 운영만 하실 거면 필요 없습니다.

```bash
git clone https://github.com/choimog/apple.git
cd apple
```

수집 프로그램을 손볼 때:
```bash
python3 -m venv .venv
.venv/bin/pip install -r crawler/requirements.txt
.venv/bin/python tests/test_match.py      # 규칙 검사
```

사이트를 손볼 때:
```bash
cd web
npm install
npm run dev        # http://localhost:3000
```

---

## 11. 알려진 한계 (정직하게)

숨기지 않고 적어둡니다.

1. **ISBN 이 대부분 없습니다.** 교보만 얻을 수 있어서, 알라딘·예스24 사이의
   매칭은 제목·저자·출판사·출간월 유사도에만 의존합니다.
   → 완벽하지 않습니다. Phase 5 에서 사람이 고칠 수 있는 화면이 필요합니다.

2. **다른 서점을 다리 삼아 잘못 묶일 수 있습니다.**
   알라딘A ─ 예스24 ─ 알라딘B 처럼요. 자동으로 갈라내면 위험해서
   `검토 필요` 로 표시만 하고 있습니다. (docs/matching-rules.md 7절)

3. **제목·저자가 둘 다 크게 다르게 적힌 책은 놓칩니다.**
   자동으로 100% 해결되지 않습니다.

4. **교보 오프라인 매장이 4곳뿐입니다.**
   대표님은 "모든 매장"을 원하셨습니다. 나머지 매장 코드는 아직 못 찾았습니다.
   `config/sources.yaml` 의 `branches` 에 추가하면 바로 수집됩니다.

5. **수집 분야가 아직 일부입니다.**
   알라딘 4개, 예스24 1개, 교보 3개. `config/sources.yaml` 에서 늘릴 수 있습니다.

6. **`db/rls.sql` 을 아직 실행하지 않았다면 데이터가 무방비 상태입니다.**
   공개용 열쇠로 데이터를 지울 수 있습니다. 배포보다 먼저 해야 할 일입니다.
   (제가 처음 만든 schema.sql 에 이 설정이 빠져 있었습니다. 제 실수입니다.)

---

## 12. 문제가 생기면 확인할 순서

1. **사이트의 `수집 상태` 화면** — 언제 실패했는지 바로 보입니다
2. **GitHub → Actions** — 빨간 ❌ 를 눌러 로그 확인
3. **Supabase → Table Editor → `crawl_logs`** — 실패 사유가 남아 있습니다

가장 흔한 원인 두 가지:
- 서점이 화면을 개편함 → `config/selectors.yaml` 수정 필요
- Supabase 무료 용량 초과 → 7절의 저장공간 대책 참고
