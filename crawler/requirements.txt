# 크롤러가 쓰는 파이썬 라이브러리 목록
# (버전을 고정해 두어야 어느 날 갑자기 동작이 바뀌는 일을 막을 수 있습니다)

httpx==0.28.1          # 웹 요청을 보내는 도구
selectolax==0.3.27     # HTML에서 값을 뽑아내는 도구 (빠르고 가벼움)
PyYAML==6.0.2          # config/*.yaml 설정 파일을 읽는 도구
supabase==2.11.0       # 데이터베이스에 저장하는 도구
playwright==1.49.1     # 화면 없는 브라우저 (교보문고 전용. 다른 서점은 안 씀)
boto3==1.35.99         # 보관소(Cloudflare R2)에 파일을 올리는 도구
gspread==6.1.4         # 구글 시트에 순위표를 쌓는 도구
google-auth==2.38.0    # 구글에 "나 맞다" 고 알리는 도구 (gspread 가 씀)
# ⚠️ 이 버전을 함부로 내리지 마세요. 0.71.0 을 쓰다가 실제로 터졌습니다
#    (2026-08-09): 코드에서 쓰는 output_config(생각 깊이 설정)가 그 버전에는
#    없어서 리포트가 실패했습니다. tests/test_report.py 가 이제 이걸 확인합니다.
anthropic==0.121.0     # AI 일일 리포트를 쓰는 도구 (Claude)
