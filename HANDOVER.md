# 인수인계 문서 (HANDOVER)

**2026-08-09 기준 · 이 문서 하나로 운영이 되도록 썼습니다.**

> 개발 지식이 없는 분을 기준으로 썼습니다. 어려운 말은 괄호로 풀었습니다.
> 다른 분에게 넘기실 때도 이 파일 하나만 주시면 됩니다.

---

## 목차

1. [이게 뭔가요](#1-이게-뭔가요)
2. [🚨 문제가 생겼을 때 — 증상별](#2--문제가-생겼을-때--증상별) ← **급하면 여기부터**
3. [매일·매주 자동으로 도는 것](#3-매일매주-자동으로-도는-것)
4. [👤 대표님이 하실 일](#4--대표님이-하실-일)
5. [자주 하실 일 — 하는 법](#5-자주-하실-일--하는-법)
6. [절대 지키고 있는 원칙](#6-절대-지키고-있는-원칙)
7. [지금 드는 돈](#7-지금-드는-돈)
8. [자료가 어디에 어떻게 쌓이나](#8-자료가-어디에-어떻게-쌓이나)
9. [파일 지도](#9-파일-지도)
10. [비밀키는 어디에](#10-비밀키는-어디에)
11. [알려진 한계 — 정직하게](#11-알려진-한계--정직하게)
12. [다른 사람에게 넘길 때](#12-다른-사람에게-넘길-때)

---

## 1. 이게 뭔가요

**교보문고 · 예스24 · 알라딘의 베스트셀러를 매일 자동으로 모아서, 세 서점 순위를
한 화면에서 비교하는 사이트입니다.**

- 대표님 PC 가 꺼져 있어도 **100% 클라우드에서 스스로** 돕니다.
- **회원만** 볼 수 있습니다 (친구분들과 비상업적으로 공유).
- 사람 손이 필요한 일은 **두세 달에 한 번 파일 내려받기** 하나뿐입니다.

### 되는 것

| | |
|---|---|
| 3사 베스트셀러 자동 수집 | 매일 새벽 6시 · 172개 분야 · **일간 300위 · 주간 500위** · 하루 약 4만 7천 권 |
| 일간 + 주간 | 둘 다 매일 모읍니다 |
| 같은 책 묶기 | 서점마다 다르게 적힌 같은 책을 하나로 (사람이 고칠 수 있음) |
| 종합 순위 | 2개 서점 이상에 오른 책을 평균 순위로 |
| 출판사별·저자별·분야별 순위 | |
| 도서 상세 | 3사 순위·판매지수 추이 그래프 |
| 정가 표시 | 웰컴·종합·서점별·출판사·작가·검색·도서상세에 정가가 함께 나옵니다 |
| 정가로 같은 책 가리기 | 도서정가제상 정가는 3사가 같아야 하므로, **정가가 다르면 다른 책**으로 봅니다 |
| 시리즈 권 번호 가리기 | 『빛과 수의 시대 1』 과 『2』 처럼 **번호가 다르면 다른 책**으로 봅니다 (2026-08-18) |
| 엑셀 내려받기 | 순위표 · 도서별 이력 |
| 회원 로그인 | 관리자 / 보기 전용 |
| 즐겨찾기 | 회원마다 담아 둔 도서의 3사 순위를 모아 보기. **각자 자기 것만** 보입니다 (2026-08-18) |
| 매칭 검토 화면 | 잘못 묶인 책을 직접 고침 (관리자만) |
| 수집 상태 화면 | 어느 날 어느 분야가 실패했는지 (**관리자만** — 2026-08-19) |
| 자동 보관 | 오래된 자료를 파일로 빼고 DB 를 비움 |
| 자동 백업 | 매주 |
| 공유 링크 | 로그인 없이 순위표 하나만 보여주는 주소 (관리자가 만들고 끔) |
| 구글 시트 자동 기록 | 매일 아침(보통 7시 30분쯤) 순위표가 시트에 쌓임 · 180일치 |
| AI 일일 리포트 | 어제 순위 요약 한 장 (**월 약 1,600원** — 실측 · 180일치 보관) |
| 리포트 알림 창 | 홈에 들어오면 하루 한 번 그날 리포트가 창으로 뜹니다 |
| 자료 끊김 경고 | 며칠째 새 자료가 없으면 모든 화면 위에 띠로 알립니다 |

### 아직 안 되는 것 (정직하게)

| | 왜 |
|---|---|
| 회원 스스로 비밀번호 바꾸기 | 안 만들었습니다. 대표님이 Supabase 에서 바꿔 주셔야 합니다 |
| 비밀번호 찾기(메일) | 무료 요금제 메일 한도 때문에 뺐습니다 |

| 교보 주간 분야별은 20위까지 | **서점 제약입니다.** 교보가 주간 세부분야는 20개만 보여줍니다 |
| 301위 밖(주간 501위 밖)은 안 모읍니다 | **2026-08-11 대표님 결정.** 무료 데이터베이스(500MB)가 하루 12MB 씩 차서 일주일이면 꽉 찰 상황이었습니다. 재 보니 저장되는 순위의 60%가 301위 이하였고, 그 뒷자리가 매일 뒤집히며 용량을 다 먹고 있었습니다. 되돌리시려면 `config/sources.yaml` 의 `max_items` 를 올리면 되는데, **용량 계산을 다시 해야 하니 먼저 말씀해 주세요** |
| 예스24 ISBN | 예스24 목록에는 ISBN 이 **없습니다** (45,031건 전부 확인). 상품번호만 있습니다. 그래서 예스24가 낀 짝은 제목·저자·출판사·정가로 판단합니다 |
| 해시태그·이벤트 문구 | 모으긴 하지만 **저장은 안 합니다.** 사이트·리포트·매칭 어디서도 안 쓰는데 24MB 를 차지하고 있었습니다. 쓰실 일이 생기면 `config/sources.yaml` 의 `save_book_meta` 를 `true` 로 |

### 주소

| | |
|---|---|
| 사이트 | Vercel 에 배포됩니다 (Vercel → 프로젝트 → Domains 에서 확인) |
| 저장소 | https://github.com/choimog/apple |
| 자동 작업 | https://github.com/choimog/apple/actions |
| 데이터베이스 | https://supabase.com/dashboard |

---

## 2. 🚨 문제가 생겼을 때 — 증상별

> **먼저 알아두실 것:** 이 시스템은 **문제가 생기면 메일이 오게** 되어 있습니다.
> GitHub 은 자동 작업이 실패하면 메일을 보냅니다. 그래서 일부러 실패시키는
> 알림도 있습니다. **빨간 X 가 전부 고장은 아닙니다.** 아래에서 구분해 드립니다.

### 증상 1) "수집 실패" 메일이 왔다

1. https://github.com/choimog/apple/actions 에서 **매일 수집** 을 엽니다
2. 빨간 X 인 실행을 눌러 로그를 봅니다
3. 아래 표에서 찾으세요

| 로그에 나오는 말 | 뜻 | 하실 일 |
|---|---|---|
| `robots.txt 가 금지` | 서점이 수집을 막았습니다 | **저에게 알려주세요.** 임의로 우회하지 않습니다 |
| `robots.txt 조차 HTTP 403` | 그 서점이 **우리 접속 자체를** 막았습니다 | 그 서점만 건너뜁니다 → [증상 1-2](#증상-1-2-한-서점만-전-분야가-통째로-실패한다-차단) |
| `0개 분야에서 도서를 못 찾음` | 서점 화면이 바뀌었습니다 | 저에게 알려주세요 (`config/selectors.yaml` 수정 필요) |
| `N개 분야가 실패` | 일부 분야만 실패 | 다음 날 자동으로 다시 시도합니다. 이틀 연속이면 알려주세요 |
| `timeout` / `429` | 서점이 느리거나 막았습니다 | 하루 기다려 보세요. 계속되면 알려주세요 |
| `서점이 **일시적으로 막은 것 같습니다**` | 그 서점이 잠시 막은 것입니다 | **설정을 건드리지 마세요.** 한두 시간 뒤 그 서점만 다시 돌리면 됩니다 (`store` 칸에 `yes24` 처럼) |
| `수집 건수가 비정상적으로 적습니다` | 받기로 한 양의 절반도 못 걷었습니다 | 한 서점·몇 개 분야뿐이면 일시적입니다. **모든 분야에서 나면** 알려주세요 |

> 한 서점이 실패해도 **나머지 두 서점은 정상 저장**됩니다. 사이트는 계속 돕니다.
>
> 💡 **예스24 는 유독 자주 막힙니다.** 같은 날 여러 번 수집하면 더 그렇습니다.
> 3사 중 예스24 에서만, 그것도 일부 분야에서만 실패한다면 화면 개편이
> 아니라 일시적인 막힘입니다. 화면이 진짜 바뀌었다면 **그 서점의 모든
> 분야가 실패**합니다. 이 둘을 구분해서 알려 드리도록 만들어 뒀습니다.

### 증상 1-2) 한 서점만 **전 분야가 통째로** 실패한다 (차단)

로그에 `차단 의심: HTTP 403` 이 그 서점 분야마다 줄줄이 나오고, 다른 두
서점은 멀쩡한 경우입니다. **2026-08-28 알라딘에서 실제로 있었습니다.**

**대표님 브라우저로는 그 서점이 잘 열립니다.** 당연합니다 — 막힌 것은
그 서점이 아니라 **우리가 접속하는 통로**입니다.

| | 대표님 | 우리 수집기 |
|---|---|---|
| 어디서 | 집·회사 인터넷 | GitHub 클라우드 컴퓨터 (데이터센터) |
| 누구라고 | 크롬 브라우저 | `BestsellerTracker/1.0` (우리 이름을 밝힘) |

서점 입장에서 이 둘은 완전히 다른 손님입니다. 사람이 쓰는 브라우저는
그대로 두고 **자동 수집만** 막는 것은 흔한 일입니다.

**🚫 우회하지 않습니다.** 브라우저인 척 이름표를 바꾸거나 다른 통로로
도는 방법이 기술적으로는 있습니다. 대표님이 처음에 **"임의로 우회하지 말고
나에게 보고해줘"** 라고 하셨고, 실무적으로도 나쁜 선택입니다 — 걸리면
더 세게, 더 오래 막힙니다.

**하실 일**

1. **자꾸 다시 돌리지 마세요.** 잠깐 막은 것이라도 두드릴수록 길어집니다.
2. **하루 기다려 보세요.** 다음 날 아침 자동 수집이 첫 번째 확인입니다.
   풀렸으면 일시적인 것이었습니다.
3. **이틀 넘게 같으면 저에게 알려주세요.** 그때는 서점에 정식으로
   문의하는 편이 맞습니다 (우리 이름표에 저장소 주소가 들어 있어서
   "이 봇입니다" 라고 밝히고 허용을 요청할 수 있습니다).

> 그동안 사이트는 **나머지 두 서점으로 계속 돕니다.** 종합 순위는
> 그날만 2사 평균이 되는데, **등락(▲▼)은 "서점 구성이 달라 견줄 수
> 없습니다" 라고 적고 견주지 않습니다.** 3사 평균과 2사 평균은 다른
> 자라서요. 웰컴의 [종합 TOP 10] 은 '3사 모두' 기준이라 그날은 빕니다.

### 증상 1-1) 수집이 안 끝나고 몇 시간째 "실행 중" 이다

빨간 X 도 아니고 초록 체크도 아닌, **노란 점이 계속 도는** 경우입니다.
실패 메일도 안 옵니다. 아무도 안 보면 그날 자료가 통째로 비어 버립니다.

**대부분 교보문고 작업의 `브라우저(크로미움) 설치` 에서 멈춘 것입니다.**
그 줄을 눌러 보시면 로그 마지막이 `archive.ubuntu.com` 어쩌고에서
멈춰 있습니다.

우리 잘못이 아닙니다. 교보문고를 읽으려면 '화면 없는 브라우저' 가
필요한데, 그걸 깔기 전에 **우분투(러너 컴퓨터의 운영체제) 꾸러미 창고**
에서 부품을 먼저 받아옵니다. 그 창고가 가끔 아주 느려집니다.
그리고 그 프로그램(apt)은 느린 창고를 **스스로 포기하지 않습니다.**

| 언제 | 그 단계가 걸린 시간 |
|---|---|
| 2026-08-18 (평소) | **36초** |
| 2026-08-19 | **88분에도 안 끝남** → 대표님이 끊으심 |

**하실 일 — 이제는 없습니다.** 2026-08-19 에 시간 제한을 걸어 뒀습니다.
- 부품 받기는 **4분** 만 기다리고, 못 받으면 경고만 남기고 넘어갑니다
  (러너에 이미 들어 있는 경우가 대부분이라 대개 그냥 돌아갑니다)
- 브라우저 본체는 **5분씩 세 번** 시도하고, 그래도 안 되면 **빨간 X** 를
  냅니다 → 실패 메일이 옵니다. 그러면 그때 다시 돌리시면 됩니다

즉, 이제 최악이어도 **20분 안에 끝나거나 빨간 X 로 알려 옵니다.**
몇 시간씩 매달려 있는 일은 없습니다.

> 그래도 또 몇 시간째 노란 점이면 — 그건 브라우저가 아니라 다른 곳입니다.
> 실행 화면 오른쪽 위 **Cancel workflow** 로 끊고 다시 돌려 보세요.
> 두 번 다 그러면 저에게 알려주세요.

### 증상 2) "보관 파일 만료 알림" 이 빨간 X 다

**고장이 아닙니다.** "파일을 내려받아 두세요" 라는 뜻입니다.
→ [4번 대표님이 하실 일](#4--대표님이-하실-일) 참고

### 증상 3) "용량 확인" 이 빨간 X 다

데이터베이스가 곧 찬다는 뜻입니다. **이건 진짜 급합니다.** 차면 수집이 멈춥니다.

로그에 무엇을 하면 되는지 적혀 있습니다. 보통은
`config/archive.yaml` 의 `keep_days` 를 줄이면 됩니다
(자료가 사라지는 게 아니라 보관 파일로 빠집니다. 사이트에서 바로 볼 수 있는
기간만 짧아집니다). 14일이 최소값입니다. 그래도 모자라면 저에게 알려주세요.

### 증상 4) 사이트가 안 열린다 / 로그인이 안 된다

| 화면에 뜨는 말 | 하실 일 |
|---|---|
| 로그인 화면만 계속 뜬다 | 비밀번호 확인. Supabase → Authentication → Users 에서 재설정 |
| `이메일이나 비밀번호가 맞지 않습니다` | 계정 만들 때 **Auto Confirm User** 를 안 켠 경우가 많습니다 |
| `데이터를 불러오지 못했습니다` | `db/auth.sql` 을 다시 실행해 보세요 (여러 번 해도 안전) |
| `데이터베이스 접속 정보가 설정되지 않았습니다` | Vercel → Settings → Environment Variables 확인 |
| 아무것도 안 뜬다 (흰 화면) | Vercel → Deployments 에서 최근 배포가 실패했는지 확인 |

### 증상 4-1) 종합에서 한 서점만 비어 있다 — 무슨 뜻인가요

같은 &lsquo;비어 있음&rsquo; 이라도 **뜻이 완전히 다릅니다.** 2026-08-12 부터
갈라서 적습니다.

| 화면 표시 | 뜻 | 어떻게 읽으면 되나요 |
|---|---|---|
| **순위 밖** | 그 서점에 이 책이 **있는데** 그날 300위(주간 500위) 안에 못 들었습니다 | **시장 신호입니다.** 그 서점에서는 덜 팔렸다는 뜻 |
| **안 묶임** | 그 서점 상품을 이 책에서 **못 찾았습니다** | **자료의 한계입니다.** 그 서점에 없거나, 있는데 아직 같은 책으로 안 묶였거나 |

> ⚠️ 둘을 섞으면 *"교보에서 안 팔리는구나"* 라고 잘못 읽게 됩니다.
> 실제로는 교보 상품을 못 찾은 것뿐일 수 있습니다.
>
> 「안 묶임」 이 자꾸 눈에 띄면 [매칭 검토] → [강제로 묶기] 로 이어
> 붙이시면 됩니다. 다음 [도서 매칭] 부터 순위가 채워집니다.

### 증상 5) 잘못 묶인 책을 발견했다

로그인 → 위쪽 **[매칭 검토]** → **자동으로 묶은 것** 탭 → **[다른 책입니다]**

**순위 화면에 반영되는 것은 다음 날 아침**입니다 (매일 새벽 계산이 한 번 돌아갑니다).

### 증상 6) 코드가 이상해졌다 / 예전 상태로 되돌리고 싶다

**2026-08-09 시점의 상태가 통째로 백업돼 있습니다.**

| 이름 | 무엇 |
|---|---|
| `backup/2026-08-09-v1.0` | 운영을 시작한 날의 코드 전체 (163개 파일) |
| `backup-20260808` | 그 전날 백업 |

**보는 법:** <https://github.com/choimog/apple/branches> 에서 이름을 누르면
그때의 파일을 그대로 볼 수 있습니다. 오른쪽 **Code → Download ZIP** 으로
PC 에 내려받을 수도 있습니다.

**되돌리는 법:** 저에게 **"백업 2026-08-09 상태로 되돌려줘"** 라고
말씀해 주세요. 지금 상태를 지우지 않고 되돌립니다.

> ⚠️ 이건 **코드** 백업입니다. **자료(순위 데이터)** 백업은 따로 있습니다
> — 매주 화요일 05:00 자동 백업 + 보관 파일. 아래 증상 6-1 을 보세요.

### 증상 6-1) 자료를 실수로 지웠다 / 이상해졌다

**되돌릴 수 있습니다.** 매주 화요일 새벽에 백업이 뜹니다.

Actions → **백업에서 되돌리기** → 되돌릴 시점을 골라 실행
(처음엔 `dry_run=true` 로 무엇이 바뀔지만 보세요)

### 증상 6-2) 하루치 자료가 엉망이라 지우고 다시 받고 싶다

수집이 여러 번 반쪽으로 돌면 그날 자료를 믿을 수 없게 됩니다.
어설프게 남겨 두면 **순위 그래프와 AI 리포트가 틀린 자료로 조용히
계산됩니다.** 그럴 때 그 날짜를 통째로 비우고 다시 받습니다.

1. Actions → **하루치 자료 지우기** → Run workflow
   - `date` : `2026-08-11` 처럼. 전부 지우려면 `all`
   - `dry_run` : 처음엔 `true` 로 얼마나 지울지만 확인
2. 숫자가 이상하지 않으면 `dry_run` 을 `false` 로 다시 실행
3. Actions → **매일 수집** → Run workflow (`store` 비움, `dry_run` `false`)

> ⚠️ **되돌릴 수 없습니다.** 지운 날짜를 다시 수집하면 '서점이 지금
> 보여주는 순위' 로 채워집니다. '어제의 어제' 를 되살릴 수는 없습니다.
>
> 🔒 대표님이 손으로 내리신 결정(같은 책 / 다른 책)이 걸린 상품은
> 지우지 않습니다. 시작·종료 시점에 건수를 세어 하나라도 줄면
> 실패로 끝냅니다.

### 증상 6-3) 순위를 더 깊게(또는 얕게) 모으고 싶다

`config/sources.yaml` 의 `max_items` 가 지금 **일간 300 · 주간 500** 입니다.
숫자를 바꾸면 다음 수집부터 반영됩니다.

**이미 쌓인 자료를 새 기준에 맞추려면** Actions → **기준 밖 순위 정리**
→ Run workflow (`dry_run` 을 먼저 `true` 로).

> ⚠️ 깊이를 올리면 용량이 그만큼 빨리 찹니다. **먼저 말씀해 주세요.**
> 무료 500MB 기준으로 1,000위까지 모으면 일주일이면 꽉 찹니다.

### 증상 7) 사이트 위에 "N일째 새 자료가 들어오지 않았습니다" 가 뜬다

**이것이 가장 중요한 경고입니다.** 다른 고장은 메일이 오지만, 이 상황만은
메일이 안 옵니다. **작업이 실패한 게 아니라 아예 안 돌기 때문입니다.**

#### ⚠️ 노란색 (2일째)

**하루 걸른 것은 가끔 있는 일입니다.** 서점 쪽이 잠깐 느렸거나 수집이
한 번 걸러진 경우입니다. **그냥 두셔도 내일 아침에 채워집니다.**

지금 바로 받고 싶으시면 [매일 수집] → **Run workflow** (1시간쯤 걸립니다).

#### 🚨 빨간색 (3일 이상)

1. **[매일 수집]** 을 엽니다
   → <https://github.com/choimog/apple/actions/workflows/daily-crawl.yml>

2. **셋 중 하나가 보입니다**

   | 보이는 것 | 뜻 | 하실 일 |
   |---|---|---|
   | 목록 맨 위에 **빨간 ✕** | 수집이 실패 중. 서점이 화면을 개편했을 가능성 | 로그를 복사해 **저에게 알려주세요** |
   | **"This workflow was disabled"** 안내와 **[Enable workflow]** 버튼 | **가장 흔한 경우.** 저장소에 두 달쯤 아무 변경이 없으면 GitHub 이 예약 작업을 스스로 끕니다. **고장이 아닙니다** | **[Enable workflow]** 를 누르시면 바로 살아납니다 |
   | 최근 실행 기록이 **아예 없음** | 위와 같은 상황 | 위와 같이. 버튼이 안 보이면 알려주세요 |

3. 어느 쪽이든 마지막에 **[Run workflow]** 를 눌러 한 번 돌려 주세요

4. 1시간쯤 뒤 사이트에서 그 띠가 사라지면 정상입니다

> **왜 GitHub 이 예약 작업을 끄나요?**
> 아무도 안 쓰는 저장소가 서버를 계속 차지하는 것을 막기 위해서입니다.
> 제가 코드를 고칠 때마다 시계가 초기화되므로, 개발 중에는 안 걸립니다.
> **개발이 끝나고 두 달쯤 지났을 때** 걸립니다. 그때 이 경고가 뜹니다.

> **이 경고는 왜 메일이 아니라 화면에 뜨나요?**
> 메일은 GitHub 이 보냅니다. 그런데 이 상황은 GitHub 쪽이 멈춘 것이라
> 메일을 보낼 주체가 없습니다. 그래서 **사이트 안에서** 판단합니다.
> 사이트는 대표님 브라우저에서 도니까 GitHub 과 상관없이 작동합니다.

---

### 증상 8) 어디에 물어봐야 할지 모르겠다

Actions 화면에서 **빨간 X 인 작업의 로그 전체를 복사해서** 저에게 주세요.
로그는 전부 한국어로 "무엇이 잘못됐고 무엇을 하면 되는지" 를 적도록 만들어 뒀습니다.

---

## 3. 매일·매주 자동으로 도는 것

**시각은 전부 한국시간입니다.**

| 언제 | 무엇 | 실패하면 |
|---|---|---|
| **매일 06:00** | 3사 수집 (`매일 수집`) | 메일. 다음 날 자동 재시도 |
| 수집이 끝나는 즉시 | 같은 책 묶기 (`도서 매칭`) | 메일. 순위는 그대로 보입니다 |
| 매칭이 끝나는 즉시 | 구글 시트에 쌓기 · AI 일일 리포트 | 메일. 사이트는 그대로 |
| 매일 수집 직후 | 용량 확인 | 메일 (한도에 가까울 때만) |

**보통 07:30 전후에 리포트까지 다 끝납니다** (실제로 잰 값: 수집 61~81분 ·
매칭 5분 · 시트와 리포트 각 1분).

> **왜 시각을 안 박아 놨나요? — 2026-08-09 대표님 요청**
> "리포트가 9시 30분까지는 나왔으면 좋겠다" 고 하셔서, 시각을 앞당기는
> 대신 **앞 작업이 끝나는 것을 보고 바로 이어서** 돌게 바꿨습니다.
> 예전에는 수집이 07:20 에 끝나도 매칭은 09:00 까지 그냥 기다렸습니다.
>
> 사슬이 어떤 이유로 안 걸린 날을 대비해 예전 시각(매칭 09:00 ·
> 시트 10:00 · 리포트 10:30)을 **보험으로 남겨 두었습니다.**
> 이미 끝난 날은 "이미 있습니다" 하고 넘어가므로 돈도 안 듭니다.
| **화 05:00** | 데이터베이스 백업 (`백업`) | 메일 |
| **화 08:00** | 오래된 자료 보관 (`보관소로 옮기기`) | 메일. 자료는 안 지워집니다 |
| **금 08:00** | 보관 파일 만료 알림 | **일부러 실패** = 파일 받으라는 뜻 |
| 코드를 고칠 때마다 | 사이트 검사 · 수집 규칙 검사 · 보안 규칙 검사 | 메일 |

**보관이 어떻게 도는지** (이게 이 시스템의 핵심 안전장치입니다):

```
1단계  파일로 뽑아냄        (데이터베이스는 손도 안 댐)
2단계  GitHub 에 올림
3단계  방금 올린 것을 다시 내려받음     ← 여기가 중요합니다
4단계  지문·줄 수가 맞는 것만 DB 에서 지움
```

"올렸다" 는 말만 믿고 지우지 않습니다. **실제로 다시 받아서 확인한 것만** 지웁니다.

---

## 4. 👤 대표님이 하실 일

### 🚨 아직 안 하신 것 (2026-08-12 기준)

아래 두 SQL 은 **아직 실행 전**입니다. 안 하시면 해당 버튼이 안 먹습니다
(누르면 "아직 준비가 안 됐습니다" 가 뜨고 **한 줄도 저장되지 않습니다**).
실행 방법은 `db/README.md` 에 있습니다.

| 파일 | 안 하면 | 걸리는 시간 |
|---|---|---|
| `db/force-join.sql` | [강제로 묶기] 버튼이 안 먹습니다 | 1분 |
| `db/publisher-alias.sql` | [출판사 묶기] 버튼이 안 먹습니다 | 1분 |
| **`db/auth.sql` (다시 실행)** | **[수집 상태] 자료가 회원에게 그대로 열려 있습니다** | 1분 |

> 앞의 둘은 안 하셔도 **수집·매칭·순위는 그대로 돕니다.** 이것 하나 때문에
> 매일 아침 작업이 멈추는 일이 없도록 만들어 두었습니다.

#### 🔒 `db/auth.sql` 을 왜 **다시** 실행해야 하나요 (2026-08-19)

대표님 요청으로 **[수집 상태]는 이제 관리자만** 볼 수 있습니다.
그런데 **문이 두 개**입니다. 하나만 잠그면 잠근 척이 됩니다.

| | 무엇이 막나 | 지금 상태 |
|---|---|---|
| 문 1 · 화면 | 회원에게는 메뉴에서 사라지고, 주소를 쳐도 안내만 뜹니다 | ✅ 배포되면 바로 |
| 문 2 · 데이터베이스 | 공개용 열쇠로 **직접 물어보는 길**을 막습니다 | ⛔ **대표님이 한 번 실행하셔야** |

문 2 가 왜 필요한가: 사이트가 쓰는 공개용 열쇠는 **브라우저 안에 그대로
들어 있습니다.** 감출 수 있는 값이 아닙니다. 사이트를 안 거치고
데이터베이스에 바로 물어볼 수 있어서, 화면만 막으면 그 길이 열려 있습니다.

**하는 법** — Supabase → SQL Editor → New query →
[`db/auth.sql`](db/auth.sql) 전체를 붙여넣고 **Run**.
(여러 번 실행해도 안전합니다. 자료는 한 줄도 안 바뀝니다)

끝나면 표가 하나 나옵니다. **4번 줄** 을 보세요.

> `4 │ 관리자만 보이는 표 (수집 기록·보관 기록) │ 2 / 2개 │ ✅ 회원에게는 안 보입니다`

⚠️ **꼭 다시 실행해야 하는 이유가 하나 더 있습니다.** 이 표들은 예전에
'회원이면 보기' 규칙을 달고 있었습니다. 보안 규칙은 여러 개면 **하나라도
통과하면 보입니다.** 그 옛 규칙을 지우는 것이 이 파일이 하는 일입니다.

### 정기적으로 — 두세 달에 한 번, 5분

**GitHub 보관 파일은 90일이 지나면 자동으로 사라집니다.** (GitHub 의 최대값이 90일입니다)

잊으셔도 됩니다. 30일 밑으로 남으면 **[보관 파일 만료 알림]** 작업이 일부러
실패해서 메일이 갑니다. 메일 안에 내려받을 주소가 들어 있습니다.

1. 메일의 주소를 열고 → 맨 아래 **Artifacts** 에서 파일 내려받기
2. PC 나 구글 드라이브에 보관
3. **알림 끄기**: Actions → **보관 파일 만료 알림** → Run workflow →
   `내려받아 저장을 마쳤습니다` 를 **true** 로 → Run

> 3번을 안 하시면 매주 같은 메일이 옵니다.

### 가끔 — 필요할 때만

- 친구분 계정 추가 → [5번 (1)](#1-친구분에게-계정을-만들어-주고-싶을-때)
- 잘못 묶인 책 고치기 → [5번 (3)](#3-같은-책인데-따로-나올-때--다른-책인데-묶였을-때)
- 수집할 분야 추가 → [5번 (4)](#4-수집할-분야매장을-추가하고-싶을-때)

---

## 5. 자주 하실 일 — 하는 법

### (1) 친구분에게 계정을 만들어 주고 싶을 때

1. https://supabase.com/dashboard → 우리 프로젝트
2. **Authentication** → **Users** → **Add user** → **Create new user**
3. 이메일 / 비밀번호(8자 이상) 입력, **Auto Confirm User** 를 **켭니다**
4. **Create user**
5. 그분께 이메일과 비밀번호를 알려 주세요

> 새 회원은 **보기 전용**입니다. 관리자로 올리려면
> SQL Editor 에서 `UPDATE profiles SET role='admin' WHERE email='그 이메일';`

> 💡 **한 사람당 계정 하나로** 주세요. 여러 명이 하나를 돌려쓰면
> 누가 보는지 알 수 없고, 한 명만 그만둬도 전부 바꿔야 합니다.

### (2) 회원을 내보낼 때

Authentication → Users → 그 계정 → **Delete user**. 즉시 못 들어갑니다.

### (3) 같은 책인데 따로 나올 때 / 다른 책인데 묶였을 때

로그인 → 위쪽 **[매칭 검토]**

| 탭 | 무엇 |
|---|---|
| 검토 대기 | 기계가 확신하지 못한 짝. 여기부터 보시면 됩니다 |
| 자동으로 묶은 것 | 확신해서 묶은 짝. **잘못 묶인 것을 여기서 떼어냅니다** |
| 내가 내린 결정 | 누르신 것. 되돌릴 수 있습니다 |

**두 가지로 좁혀 볼 수 있습니다** (2026-08-09 추가).

**① 묶인 권수** — 서점이 셋이라 **3권이 정상**입니다.

| 버튼 | 뜻 |
|---|---|
| 2권 이하 | 한 서점을 놓쳤을 수 있습니다 |
| 3권 (정상) | 세 서점에서 한 권씩 |
| **4권 이상** | 🚨 **한 서점에서 두 권이 묶였다는 뜻**입니다. 개정판·세트가 섞였을 가능성이 큽니다 |

**여기서 [4권 이상] 부터 보시면 잘못 묶인 책을 가장 빨리 찾습니다.**
카드마다 "N권 묶임" 이 적혀 있고, 4권 이상은 노란색으로 표시됩니다.

**② 점수로 좁혀 보기**
탭 아래 **[전체] [65~69점] [70~74점] …** 버튼을 누르면 그 점수대만 보입니다.
버튼 옆 숫자가 그 점수대에 몇 짝 있는지입니다.

- **검토 대기**는 **낮은 점수부터** 보세요. 기계가 가장 덜 확신한 것들입니다
- **자동으로 묶은 것**은 **85~89점**이 가장 위험합니다. 자동 병합 기준선을
  겨우 넘긴 짝이라, 잘못 묶였다면 여기 있을 가능성이 큽니다
- 0짝인 점수대는 버튼이 아예 안 보입니다

**③ 엑셀로 한꺼번에** (2026-08-09 추가) — 하나씩 누르기 힘드실 때

1. 화면에서 조건(탭·점수·묶인 권수)을 걸고 **[검토 목록 내려받기]**
2. 엑셀에서 **결정** 칸에 `같은책` 또는 `다른책` 만 적습니다
   (빈칸은 그냥 넘어갑니다. 전부 채우지 않으셔도 됩니다)
3. **CSV(쉼표로 분리)** 로 저장 → **[올려서 반영]**
4. "✅ N건 반영했습니다" 처럼 **숫자로** 결과가 나옵니다

> 다른 파일을 올리시면 **한 줄도 반영하지 않고** 왜 안 되는지 알려 드립니다.
> `되돌리기` 라고 적으면 기계 판단으로 되돌아갑니다.

**④ 규칙이 갈라 놓은 것을 직접 잇기** (2026-08-12 추가)

위 세 탭은 **전부 이미 이어진 짝**입니다. 규칙이 &lsquo;다른 책&rsquo; 이라고
판정한 짝은 저장조차 안 되어 있어서 거기서는 손댈 수가 없습니다.
그래서 버튼을 둘 더 두었습니다.

| 버튼 | 언제 쓰나요 |
|---|---|
| **[강제로 묶기]** | 「안녕이라 그랬어 (집 에디션)」 / 「…(집에디션 리커버)」 처럼 규칙상 반드시 갈라지지만 같은 책일 때. 제목으로 찾아 2~6권을 골라 한 번에 묶습니다 |
| **[출판사 묶기]** | `청림Life` / `청림라이프` 처럼 서점마다 표기가 달라 글자로는 못 잡을 때. 한 번 정하면 그 출판사 책 전부에 적용됩니다 |

> ⚠️ 둘 다 **처음 한 번 SQL 을 실행하셔야** 버튼이 먹습니다.
> `db/force-join.sql` · `db/publisher-alias.sql` (→ `db/README.md`)
> 안 하시고 누르면 "아직 준비가 안 됐습니다" 라고 뜨고 **한 줄도 저장되지
> 않습니다.** 조용히 실패하지 않게 해 두었습니다.

> [출판사 묶기] 는 **규칙을 건드리지 않습니다.** 순위·점수는 매일 처음부터
> 다시 계산하므로 [풀기] 를 누르면 다음 [도서 매칭] 에서 정확히 원래대로
> 돌아갑니다. 다만 **진짜로 다른 출판사를 묶으면 그 책들이 섞입니다**
> (창비 / 창비교육 처럼요). 그건 기계가 막을 수 없습니다.

**대표님이 누르신 결정은 기계가 절대 못 뒤집습니다.**
순위 화면 반영은 **다음 날 아침**입니다.

> 전체적으로 자꾸 틀린다면 `config/matching.yaml` 의 점수 기준을 조정해야 합니다.
> (지금: 85점 이상 자동 병합 / 65점 이상 검토 대기 / 그 미만 다른 책)
>
> **즉시 거부 규칙 — 점수로는 절대 못 뒤집습니다.** (자주 걸리는 것만
> 적었습니다. 여덟 가지 전부는 `docs/matching-rules.md` 3절에 있습니다)
> ① 제목이 60% 미만으로 닮음 ② 출판사가 다름
> ③ **출간월(배본일)이 한 달이라도 다름** (2026-08-09 · 대표님 지시)
> ④ 정가가 다름 (2026-08-11 · 대표님 지시)
> ⑤ **시리즈 권 번호가 다름** — 『빛과 수의 시대 1』 과 『2』
>    (2026-08-18 · 대표님 지시). 양쪽 다 번호가 적혀 있을 때만 갈라냅니다
>
> ⚠️ ③ 때문에 **서점이 배본일을 다르게 적은 같은 책은 갈라집니다.**
> 예: 『아버지의 해방일지』 예스24 2022-09 / 알라딘 2022-10 → 2권으로.
> 되돌리시려면 `config/matching.yaml` 의 `pub_ym_near_months` 를 `1` 로
> 바꾸고 [도서 매칭] 을 한 번 돌리시면 됩니다.
> 이건 저에게 알려주세요. 한 줄만 바꿔도 수만 권이 영향을 받습니다.

### (4) 수집할 분야·매장을 추가하고 싶을 때

1. GitHub → `config/sources.yaml` 을 엽니다
2. 연필(✏️) 아이콘 → 비슷한 항목을 복사해서 아래에 붙이고 값만 고칩니다
3. **Commit changes**
4. 저장하면 **설정 점검** 작업이 자동으로 돌아서 주소가 살아 있는지 확인합니다

> 분야 코드를 모르실 때: Actions → **분야·매장 코드 찾기** 를 실행하면
> 서점에서 코드 목록을 뽑아 줍니다.

### (5) 지금 당장 수집을 돌리고 싶을 때

Actions → **매일 수집** → **Run workflow**
- `store` 를 비우면 3사 전부, `aladin` 처럼 적으면 그 서점만
- `dry_run` 을 `true` 로 두면 저장하지 않고 확인만

### (6) 계정 없는 분에게 순위표 하나만 보여주고 싶을 때

로그인 → 위쪽 **[공유 링크]** → 분야를 고르고 **[주소 만들기]**

**회원 누구나 만들 수 있습니다** (2026-08-09 · `db/share-open.sql` 실행 후).
회원은 자기가 만든 것만 보고 끕니다. 대표님은 **전부 보이고, 누가 만들었는지도
이메일로 보입니다.** 누구 것이든 끄실 수 있습니다.
회원은 **동시에 2개까지 · 최대 3시간** 기한입니다 (대표님은 제한 없음).
시간이 지나면 저절로 꺼지므로 주소가 돌아다녀도 오래 열려 있지 않습니다.

- 만들어진 주소(`https://…/s/64글자`)를 복사해서 보내주시면 됩니다
- 그 주소로는 **그 분야 순위표 하나만** 보입니다. 다른 화면은 못 봅니다
- 기본 30일 뒤 자동으로 꺼집니다 (7/30/90일·기한없음 중에 고르실 수 있습니다)
- 언제든 목록에서 **[끄기]** 를 누르면 즉시 안 열립니다

> 검색엔진에 안 올라가게 막아 두었습니다. 주소를 받은 분만 볼 수 있습니다.

### (7) 특정 날짜 자료를 다시 보고 싶을 때 (보관소에서)

Actions → **보관소에서 불러오기** → 날짜 지정
(`dry_run=true` 로 먼저 무엇을 불러올지 보세요)

### (8) 구글 시트에 다른 표도 넣고 싶을 때

`config/sheets.yaml` 에 한 덩어리를 더하면 시트 탭이 하나 늘어납니다.
고치기 어려우시면 **"이 표도 시트에 넣어줘"** 라고 말씀만 주세요.

> 아직 시트를 연결 안 하셨다면 [`docs/sheets-setup.md`](docs/sheets-setup.md)
> 를 따라 10분만 쓰시면 됩니다. 돈은 들지 않습니다.

---

## 6. 절대 지키고 있는 원칙

대표님이 처음에 못 박으신 것들입니다. 코드에 그대로 반영돼 있습니다.

| 원칙 | 어떻게 지키고 있나 |
|---|---|
| **서점 공식 API 사용 금지** | 알라딘 TTB 포함, 어떤 공식 API 도 안 씁니다 |
| **도서 상세 페이지 진입 금지** | 목록 페이지만 읽습니다. 예외 없음 |
| **표지 이미지 저장 금지** | 주소만 문자열로 보관. 브라우저가 서점에서 직접 불러옵니다 |
| **robots.txt 준수** | 매 실행마다 다시 확인. 금지면 그 서점 수집을 중단하고 보고합니다 |
| **요청 간격 1~2초** | 동시 접속 없이 한 번에 하나씩 |
| **가짜 데이터 금지** | 없는 값은 비워 둡니다. 실패는 실패로 기록합니다 |
| **비밀키 코드에 금지** | 환경변수·Secrets 에만 |
| **조용한 실패 금지** | 수집량이 평소 절반 미만이면 성공이 아니라 **실패**로 기록 |
| **부분 실패 허용** | 한 서점이 실패해도 나머지 서점 자료는 그대로 저장됩니다 |
| **멱등성** | 같은 날 몇 번을 다시 돌려도 자료가 중복되지 않습니다 |
| **지우기 전에 확인** | 보관 작업은 올린 파일을 **실제로 다시 내려받아** 지문과 줄 수가 맞는 것만 데이터베이스에서 지웁니다 |

### ⚠️ 교보문고에 대한 예외 — 대표님 승인 사항 (2026-08-07)

교보 베스트셀러 자료는 **robots.txt 가 금지한 `/api/gw` 경로에서만** 나옵니다.
허용된 다른 입구가 있는지 확인했지만 **없었습니다** (www / mobile / product 모두 404).

이 사실을 보고드렸고, 대표님이 **"헤드리스 브라우저로 진행"** 을 선택하셨습니다.
브라우저를 띄우면 페이지의 자바스크립트가 그 경로를 자동으로 호출합니다.

대신 예의는 더 엄격하게 지킵니다:
- 신원을 밝히는 User-Agent
- 페이지 사이 1.5~2.5초, 동시 접속 없음
- 광고·추적 요청은 차단, 이미지는 받지 않음(주소만 읽음)

> 되돌리시려면 `crawler/run_daily.py` 의 `BROWSER_STORES` 에서 `"kyobo"` 를
> 빼면 교보만 멈추고 나머지는 그대로 돕니다.

---

## 7. 지금 드는 돈

| 항목 | 요금제 | 월 비용 |
|---|---|---|
| GitHub Actions | 무료 (공개 저장소는 무제한) | **0원** |
| Supabase | 무료 (500MB) | **0원** |
| Vercel | 무료 (Hobby) | **0원** |
| 구글 시트 내보내기 | 무료 | **0원** |
| AI 일일 리포트 | 켜셨을 때만 · 승인 완료 | **약 2,500원** (한도 4,200원) |
| **합계** | | **0원** (리포트를 켜시면 약 2,500원) |

> 📌 리포트 값이 1,600원 → 2,500원으로 오른 이유 (2026-08-12)
> 대표님 요청으로 **지난 7일치 리포트를 같이 읽고** 쓰게 했습니다.
> 넣는 글이 길어진 만큼 하루 약 28원이 더 듭니다.
> 한도(4,200원)에 부담이 커지면 프로그램이 **스스로 날수를 줄이고
> 그 사실을 작업 기록에 적습니다.** 조용히 줄이지 않습니다.
> 되돌리시려면 `config/report.yaml` 의 `history_days` 를 `0` 으로.

**돈이 드는 곳은 AI 리포트 하나뿐입니다.** 그리고 그것도 한도가 걸려 있어
`config/report.yaml` 의 `monthly_cap_usd`($3.00 · 약 4,200원)를 넘길 수 없습니다.
한도에 닿으면 그 달은 리포트만 멈추고 나머지는 그대로 돕니다.

### 앞으로 돈이 들 수 있는 지점

**(1) Supabase 용량** — 지금 약 192MB / 500MB.
자동 보관(14일치만 남김)으로 관리하고 있고, 매일 확인해서 위험하면 메일이 갑니다.
정말 모자라면 Pro(월 약 35,000원) 또는 보관 기간 단축을 정하시면 됩니다.

**(2) Vercel 약관** — 지금은 **비상업적** 사용이라 무료(Hobby) 가 맞습니다.
나중에 **출판 마케팅 업무에 쓰기 시작하시면** 약관 위반이 됩니다. 그때는
Cloudflare Pages 로 옮기면(0원, 상업적 이용 허용) 됩니다. 반나절 작업입니다.

**(3) AI 일일 리포트** — 월 약 3,300원. 지금은 빼 두었습니다.

---

## 8. 자료가 어디에 어떻게 쌓이나

### 지금 규모 (2026-08-09 실측)

| 표 | 내용 | 건수 |
|---|---|---|
| `categories` | 수집 대상 분야·매장 | 208개 |
| `rankings` | 날짜별 순위 (**날마다 쌓임**) | 230,590건 |
| `store_books` | 서점별 도서 (새 책이 나올 때만 늚) | 95,461건 |
| `books` | 같은 책끼리 묶은 도서 마스터 | 64,785건 |
| `book_matches` | 왜 묶었는지 근거 + 사람의 결정 | |
| `crawl_logs` | 언제 무엇을 수집했고 성공했는지 | 1,325건 |
| `profiles` | 회원과 권한 (admin / viewer) | |
| `archives` | 어느 날짜를 어디로 옮겼는지 | |

### 용량이 어떻게 늘어나나 (중요)

표는 성격이 **두 가지**입니다. 이걸 섞으면 계산이 크게 틀립니다.

| | 표 | 어떻게 늘어나나 | 보관소로 빠지나 |
|---|---|---|---|
| **날마다 쌓이는 것** | `rankings`, `book_meta` | 하루치씩 정직하게 | ✅ 빠집니다 |
| **도서 목록** | `books`, `store_books` 등 | 처음 보는 책이 나올 때만 | ❌ 안 빠집니다 |

첫날에는 7만 권이 전부 '처음 보는 책' 이라 목록이 폭발합니다. 그 뒤로는 거의 안 늡니다.
**그래서 "전체 용량 ÷ 날짜 수" 로 하루 증가량을 내면 안 됩니다.**
(2026-08-09 에 실제로 이렇게 계산해서 "3일 뒤 꽉 참" 이라는 틀린 경고가 나갔습니다)

지금 계산식:

```
예상 최대치 = 도서 목록 + (하루 순위 용량 × 보관 일수)
            = 약 148MB  + (약 22MB × 14일)  ≈ 456MB   (한도 500MB)
```

### 보관 흐름

```
최근 14일   Supabase 에 있음 → 사이트에서 바로 보임
그 이전     GitHub 보관 파일 → 90일 안에 내려받아야 함
            (필요하면 [보관소에서 불러오기] 로 되살릴 수 있음)
```

---

## 9. 파일 지도

```
config/            ★ 운영 중 고칠 일이 있는 건 대부분 여기입니다
  sources.yaml       수집할 서점·분야 주소 (분야 추가/삭제)
  selectors.yaml     서점 화면에서 값을 뽑는 규칙 (서점이 개편하면 여기)
  matching.yaml      같은 책 묶기 점수 기준
  archive.yaml       보관 기간(순위 keep_days · 기록/리포트 log_keep_days)
  sheets.yaml        구글 시트에 내보낼 표 목록
  report.yaml        ★ AI 리포트 — 한 달 쓸 수 있는 최대 금액이 여기 있습니다

crawler/           수집·매칭 프로그램 (파이썬)
  run_daily.py       매일 수집
  run_match.py       같은 책 묶기
  archive.py         보관소로 옮기기 / 만료 알림
  backup.py          백업
  capacity.py        용량이 언제 차는지
  export_sheets.py   구글 시트에 쌓기
  report_data.py     리포트에 넣을 숫자 뽑기 (AI 안 부름)
  run_report.py      AI 일일 리포트 (돈을 쓰는 유일한 곳)
  restore.py         보관소에서 불러오기
  reset_data.py      자료 정리 (날짜 삭제)
  verify_publishers.py  출판사가 섞이지 않았는지 확인
  common/            공통 규칙 (요청 예의, 매칭 계산, DB 접속)
  stores/            서점별 수집 코드

db/                Supabase 에서 실행하는 SQL (붙여넣고 Run)
  schema.sql         표 만들기 (맨 처음 한 번)
  rls.sql            기본 보안 잠금
  auth.sql           ★ 회원 전용 + 관리자 권한
  archive_schema.sql 보관 기록표
  perf.sql           속도 개선 (화면이 느릴 때)
  check.sql          무엇이 빠졌는지 진단

web/               사이트 (Next.js)
  app/               화면들
  lib/               자료를 꺼내오는 코드
  components/        화면 부품
  middleware.ts      ★ 로그인 안 한 사람 막기
  scripts/           자동 검사

tests/             규칙이 깨지지 않았는지 확인하는 시험들
docs/              설명 문서
  site-guide.md      ★ 사이트 안내서 — 화면에서 뺀 설명이 전부 여기
  roadmap.md         남은 일
  login-setup.md     회원 전용 켜는 법
  archive-setup.md   보관소 설명
  sheets-setup.md    ★ 구글 시트 연결하는 법 (10분)
  ai-report-setup.md ★ AI 리포트 켜는 법 (3분 · 돈이 듭니다)
  matching-rules.md  같은 책 묶기 규칙
  deploy.md          배포 설정
  cost-plan.md       비용 계획

.github/workflows/ 자동으로 도는 작업들
.github/actions/
  browser/         교보문고용 브라우저 까는 법 (여섯 작업이 이걸 같이 씁니다)
```

---

## 9-1. 저장소를 공개로 두는 것에 대해

**공개로 두는 이유는 하나입니다 — GitHub Actions 가 무료·무제한이기 때문입니다.**
비공개로 바꾸면 월 2,000분 한도가 생기는데, 지금 쓰는 양은 하루 약 90분
(월 2,700분)이라 **한도를 넘어 요금이 붙습니다.**

### 공개라서 남에게 보이는 것

| 보이는 것 | 문제인가 |
|---|---|
| 코드 전부 | ❌ 아닙니다. 남이 베껴도 대표님 데이터베이스는 못 씁니다 |
| 실행 기록(Actions 로그) | ⚠️ **누구나 읽습니다.** 아래를 보세요 |
| 수집한 순위·책 제목 일부 | ❌ 서점에 공개된 정보입니다 |
| 데이터베이스 안의 자료 | ❌ 안 보입니다. 열쇠가 있어야 합니다 |

### 비밀키는 공개돼도 안전합니다

GitHub Secrets 에 넣은 값은 **공개 저장소에서도 남에게 안 보입니다.**
로그에 실수로 찍혀도 GitHub 이 `***` 로 가립니다.
남이 이 저장소를 복제(fork)해서 작업을 돌려도 **열쇠는 안 넘어갑니다.**

### 🚨 딱 하나 고친 것 (2026-08-09)

`crawler/run_report.py` 가 **AI 리포트 전문을 로그에 찍고 있었습니다.**
사이트는 회원만 보게 잠가 놓고, 정작 같은 글이 누구나 읽는 로그에
그대로 있었습니다. 잠근 의미가 없었습니다.

이제 첫 줄과 글자 수만 찍습니다. 전문은 사이트에서만 보입니다.

### 앞으로 조심할 것

- 로그에 **회원 이메일·비밀번호·리포트 전문** 같은 것을 찍지 마세요
  (제가 코드를 고칠 때 지키겠습니다. 대표님이 하실 일은 없습니다)
- 남이 보낸 Pull Request 는 **내용을 확인하기 전에는 승인하지 마세요**
- 저장소 이름·설명에 회사 내부 정보를 적지 마세요

---

## 10. 비밀키는 어디에

**코드에는 한 줄도 없습니다.** 두 곳에만 있습니다.

### GitHub Secrets (수집 작업이 씀)

Settings → Secrets and variables → Actions

| 이름 | 무엇 |
|---|---|
| `SUPABASE_URL` | 데이터베이스 주소 |
| `SUPABASE_SERVICE_ROLE_KEY` | **관리자 열쇠.** 절대 외부에 노출 금지 |
| `SUPABASE_ANON_KEY` | 공개용 열쇠 (검사용) |
| `SMOKE_EMAIL` / `SMOKE_PASSWORD` | (선택) 검사용 계정 |
| `GOOGLE_SERVICE_ACCOUNT_JSON` | (선택) 구글 시트 로봇 계정 신분증 — 없으면 시트 내보내기만 쉼 |
| `GOOGLE_SHEET_ID` | (선택) 쌓아 넣을 구글 시트 주소의 가운데 글자 |
| `ANTHROPIC_API_KEY` | (선택) AI 리포트 열쇠 — 없으면 리포트만 쉽니다 |

### Vercel Environment Variables (사이트가 씀)

| 이름 | 무엇 |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | 데이터베이스 주소 |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | **공개용 열쇠만.** 관리자 열쇠는 절대 여기 넣지 마세요 |

> 공개용 열쇠는 브라우저 안에 그대로 들어갑니다. 감출 수 없는 값입니다.
> 그래서 **데이터베이스 쪽에서** 로그인한 사람만 읽게 잠가 두었습니다(`db/auth.sql`).
> 자동 검사가 매번 "로그인 없이 자료가 새어 나가는지" 를 확인합니다.

---

## 11. 알려진 한계 — 정직하게

| 한계 | 내용 |
|---|---|
| **판매지수는 교보만 없음** | 교보가 공개하지 않습니다. 빈 칸으로 둡니다 (추정하지 않음) |
| **ISBN 은 교보만 있음** | 다른 두 서점은 목록에 노출하지 않습니다 |
| **출간월이 없는 책** | 목록에 안 나오면 비워 둡니다 |
| **같은 책 묶기는 100%가 아님** | 애매한 것은 '검토 대기' 로 남깁니다. 사람이 고칠 수 있습니다 |
| **너무 큰 무리는 못 가름** | 60권 넘는 무리에서 [다른 책] 을 눌러도 안 갈라집니다. 이때는 **로그에 크게 알립니다** |
| **보관 파일은 90일** | GitHub 최대값. 사람이 내려받아야 합니다 |
| **회원 비밀번호 자가 변경 불가** | 대표님이 Supabase 에서 바꿔 주셔야 합니다 |
| **결정 반영은 다음 날** | 매칭 계산은 하루 한 번 돕니다 |
| **서점 개편에는 사람 손이 필요** | 화면 구조가 바뀌면 `selectors.yaml` 수정이 필요합니다. 다만 **조용히 빈 데이터를 저장하지 않고 실패로 알립니다** |

---

## 12. 다른 사람에게 넘길 때

넘겨받는 분에게 이것만 주시면 됩니다.

1. **이 문서** (HANDOVER.md)
2. **GitHub 저장소 접근 권한** — Settings → Collaborators
3. **Supabase 프로젝트 접근 권한** — Settings → Team
4. **Vercel 프로젝트 접근 권한**

### 새 PC 에서 코드를 고치려면

```bash
git clone https://github.com/choimog/apple.git
cd apple

# 파이썬 쪽 (수집)
python -m venv .venv && source .venv/bin/activate
pip install -r crawler/requirements.txt

# 사이트 쪽
cd web && npm ci
```

**코드를 안 고쳐도 운영은 됩니다.** 위 1~4번만 있으면 GitHub 웹 화면에서
설정 파일을 고치고 작업을 실행할 수 있습니다.

### 규칙이 안 깨졌는지 확인하는 법

```bash
python tests/test_match.py            # 같은 책 묶기 규칙
python tests/test_manual_split.py     # 사람의 결정이 실제로 반영되는지
python tests/test_capacity.py         # 용량 예측
python tests/test_archive_roundtrip.py  # 보관 파일이 안 깨지는지
bash   tests/test_auth_sql.sh         # 보안 규칙이 진짜로 막는지
python tests/test_browser_install.py  # 브라우저 설치가 무한정 안 기다리는지
cd web && node scripts/test-csv.mjs   # 엑셀 내려받기
```

이 시험들은 **코드를 올릴 때마다 자동으로도** 돌아갑니다.

---

## 마지막으로

이 시스템에서 **가장 위험한 것은 고장이 아니라 '조용한 고장'** 입니다.
수집이 안 됐는데 성공으로 보이거나, 잠금이 풀렸는데 화면은 멀쩡하거나,
용량 경고가 틀려서 매번 빨간불이거나 하는 것들입니다.

그래서 곳곳에 **"조용히 넘어가지 않게" 하는 장치**를 넣어 두었습니다.
빨간 X 가 뜨면 귀찮으시겠지만, **그게 이 시스템이 정직하게 동작하고 있다는 신호**입니다.
